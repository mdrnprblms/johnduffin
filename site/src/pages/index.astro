---
import Layout from '../layouts/Layout.astro'
import {withBase} from '../lib/paths'

const HERO_IMAGE_URL =
  'https://static-assets.artlogic.net/w_1200,c_limit,f_auto,fl_lossy,q_auto/artlogicstorage/bg/images/view/0403b41a4d3dafe2b2bf17dc1df29f10/banksidegallery-john-duffin-rws-re-thames-bridges-east-sunrise.jpg'
---

<Layout title="Home" fullBleed>
  <div class="hero">
    <div class="hero-image" style={`background-image: url(${HERO_IMAGE_URL})`}></div>
    <div class="links">
      <a class="button" href={withBase('/portfolio')}>Work</a>
    </div>
  </div>
</Layout>

<style>
  .hero {
    position: relative;
    flex: 1;
    display: flex;
  }

  .hero-image {
    flex: 1;
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
  }

  .links {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .button {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background: var(--ink);
    color: var(--bg);
    text-decoration: none;
    border-radius: 2px;
    letter-spacing: var(--tracking);
    font-size: 0.9rem;
  }
</style>
