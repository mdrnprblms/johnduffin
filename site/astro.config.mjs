// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static',
  // TODO: set these once the GitHub repo exists, e.g.
  // site: 'https://<github-username>.github.io',
  // base: '/<repo-name>',
});
